2025-08-27
大模型满天飞的时代，真相和假相很容易就被混杂在一起。使用DESeq2做差异分析已经有一段时间，我想是时候写下一些我对DESeq2的理解，
首先能够帮助我理解差异分析的本质，其次是发掘中文互联网，只有几年前NTU的一篇对DESeq2背后逻辑的解读，我觉得这种声音可以更多一些，从
多个侧面看清楚差异分析到底在做什么。

在初级层面来看，这个包就是输入一个表达矩阵（包括metadata），指定要对比的condition，最后拿到差异基因表格，但是这不是本文关注的重点，
本文想要关注的是它如何从这个表达矩阵拿到差异基因表格，以及为什么要这么做。

回到 DESeq2 发表的 2014 年，当时转录组差异分析主要面临两个问题：
第一，条件（condition）下的样本数过少（n ＜ 3）。在使用学生 t 检验时，如果 n ＜ 2，检验无法进行；即便 n=2，自由度过低（df ≤ 1）
，t 分布的“离散度”（此处指其方差或标准差）在理论上为无穷大，导致统计推断极不可靠。
第二，在用 Log2FC 衡量变化差异时，如果数据本身方差很大，∣log2​(A/B)∣>0 的比值几乎没有意义：方差大时，Log2FC 的波动
会非常大，即便表面上倍数变化明显，也可能完全是噪声；尤其当 A 或 B 中任意一方的 read count 很小时，相除甚至可能得出方向相反的结果。


DESeq2 的核心设计目标，就是同时对离散度估计和 Log2FC 进行经验贝叶斯收缩（shrinkage），以获得更稳健（robust）的差异分析结果。
经验贝叶斯（Empirical Bayes）的核心思想，是利用所有基因的整体信息来改善单个基因的参数估计：先依据全局数据拟合“均值-离散度”关系曲
线作为先验分布（prior），再结合该基因的观测数据，得到折中的后验估计（posterior estimate）。

这一方法隐含的核心假设是：各基因的方差（或离散度）来自相同或相似的分布，因此全体基因可共享这一先验结构，从而在小样本或高波动数据下获得
更稳定的估计。

在 DESeq2 中，观测到的 read count 被建模为服从负二项分布（Negative Binomial），离散度参数（dispersion）的先验分布可近似为对数
正态分布（log-normal）。经验贝叶斯方法会将每个基因的离散度估计值向全局“均值-离散度”趋势进行收缩（shrinkage），以减少小样本和异常波
动造成的不稳定性。
